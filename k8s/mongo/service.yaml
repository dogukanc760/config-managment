apiVersion: v1
kind: Service
metadata:
  name: mongo
  namespace: todo
spec:
  selector:
    app: mongo
  ports:
    - port: 27017
      targetPort: 27017
  clusterIP: None  # Headless service — StatefulSet için gerekli
